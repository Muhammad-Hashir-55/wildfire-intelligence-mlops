name: Wildfire MLOps CI Pipeline

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    # 1. Checkout Code
    - uses: actions/checkout@v3

    # 2. Set up Python
    - name: Set up Python 3.9
      uses: actions/setup-python@v4
      with:
        python-version: "3.9"

    # 3. Install Dependencies
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    # 4. Run Data Validation
    - name: Run DeepChecks Drift Detection
      run: python tests/test_model.py

    # 5. Run API Unit Tests
    - name: Run API Unit Tests
      run: python tests/test_api.py

    # 6. DISCORD NOTIF - SUCCESS
    # We use ${{ secrets.DISCORD_WEBHOOK }} because the file doesn't exist online
    - name: Discord Notification - Success
      if: success()
      uses: Ilshidur/action-discord@master
      env:
        DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
      with:
        args: '✅ BOOM! CI Pipeline Passed! System is Certified! 🚀🔥'

    # 7. DISCORD NOTIF - FAILURE
    - name: Discord Notification - Failure
      if: failure()
      uses: Ilshidur/action-discord@master
      env:
        DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
      with:
        args: '❌ ERROR! Build Failed! Check the logs! 💀⚠️'