version: '3.8'

services:
  # 1. FastAPI Backend
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: wildfire_api
    ports:
      - "8000:8000"
    volumes:
      # Sync Code (so you can edit locally)
      - ./app:/app/app
      - ./src:/app/src
      # Sync Models (so the container saves .pkl files to your laptop)
      - ./models:/app/models
      # CRITICAL: Give container access to Data for training
      - ./data:/app/data
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/wildfire_db
    depends_on:
      - db
    networks:
      - wildfire_network

  # 2. Streamlit Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: wildfire_ui
    ports:
      - "8501:8501"
    environment:
      - API_URL=http://api:8000
    depends_on:
      - api
    networks:
      - wildfire_network

  # 3. PostGIS Database
  db:
    image: postgis/postgis:15-3.3
    container_name: wildfire_db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: wildfire_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - wildfire_network

volumes:
  postgres_data:

networks:
  wildfire_network:
    driver: bridge